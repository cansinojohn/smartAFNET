# smartAFNET
Built and deployed a deep learning–based refraction error detection system using transfer learning (ResNet50 + REDNet), converted to TensorFlow Lite for real-time, on-device single-image inference via smartphone camera in an Android app (“iTest”).
